{
  "name": "capstonedemo",
  "version": "1.0.0",
  "private": true,
  "description": "CapstoneDemo API with Supabase (PostgreSQL)",
  "type": "module",
  "scripts": {
    "start": "npm --prefix server start",
    "dev": "npm --prefix server run dev",
    "check": "npm --prefix server run check"
  }
}
